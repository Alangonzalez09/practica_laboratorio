#include <stdio.h>
int main(){
	int num,sum=0,i;
	for(i=0;i<501;i++){sum = (sum +(2*i));
}
printf("\nla suma de todos los múltiplos de 2 hasta el 1000, es: %d",sum);sum =0;
	for(i=0;i<143;i++){sum = (sum +(7*i));
}
printf("\nla suma de todos los múltiplos de 7 hasta el 1000, es: %d",sum);
return 0;
}
